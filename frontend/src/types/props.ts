/**
 * Common prop interfaces for SpendMila components
 */

export interface AddTransactionProps {
  isOpen?: boolean;
  onOpenChange?: (open: boolean) => void;
}

export interface AddAccountProps {
  isOpen?: boolean;
  onOpenChange?: (open: boolean) => void;
}

export interface AddDebtProps {
  isOpen?: boolean;
  onOpenChange?: (open: boolean) => void;
}

export interface AddGoalProps {
  isOpen?: boolean;
  onOpenChange?: (open: boolean) => void;
}

export interface AddSubscriptionProps {
  isOpen?: boolean;
  onOpenChange?: (open: boolean) => void;
}

export interface CreateBudgetProps {
  isOpen?: boolean;
  onOpenChange?: (open: boolean) => void;
}
